command: list
resource: images
available_only: true
api:
  base_url: https://cloud.lambda.ai/api/v1
  api_key: ${oc.env:LAMBDA_API_KEY}
  timeout: 120
cloud_init:
  template_path: null
  use_default: true
wait:
  timeout: 900
  poll_interval: 5.0
ssh:
  username: ubuntu
